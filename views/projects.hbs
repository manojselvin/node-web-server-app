<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>{{pageTitle}}</title>
</head>
<body>
    {{> header}}

    <p>Projects list</p>
    <ul class="project_list">
        {{#each projectsList}}
            <li><h1>#{{this.projectId}}</h1>{{this.projectName}}</li>
        {{/each}}
    </ul>

    {{> footer}}
</body>
</html>